<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>TikTok Quote Generator · Xaerisoft</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            background: #050308;
            color: #e8eef5;
            font-family: -apple-system, 'Segoe UI', system-ui, sans-serif;
            min-height: 100vh;
            overflow-x: hidden;
            display: flex;
            flex-direction: column;
        }

        /* BACKGROUND EFFECTS */
        #particles {
            position: fixed;
            top: 0; left: 0;
            width: 100%; height: 100%;
            z-index: 0;
            pointer-events: none;
        }
        .glow-orb {
            position: fixed;
            border-radius: 50%;
            filter: blur(100px);
            opacity: 0.2;
            z-index: 0;
            pointer-events: none;
            animation: float 15s ease-in-out infinite;
        }
        .orb-1 { width: 400px; height: 400px; background: #8a5aff; top: -100px; left: -100px; }
        .orb-2 { width: 350px; height: 350px; background: #ff5a8a; bottom: -100px; right: -100px; animation-delay: -5s; }
        @keyframes float {
            0%, 100% { transform: translate(0, 0) scale(1); }
            50% { transform: translate(50px, -50px) scale(1.1); }
        }

        /* NAVBAR */
        .navbar {
            position: sticky;
            top: 0;
            z-index: 100;
            padding: 16px 28px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 1px solid rgba(138, 90, 255, 0.15);
            backdrop-filter: blur(20px);
            background: rgba(5, 3, 8, 0.8);
        }
        .navbar .logo {
            font-size: 20px;
            font-weight: 800;
            background: linear-gradient(135deg, #b08aff, #ff8a5a);
            -webkit-background-clip: text;
            background-clip: text;
            -webkit-text-fill-color: transparent;
            letter-spacing: 1px;
            text-decoration: none;
        }
        .navbar .logo span { color: #e8eef5; -webkit-text-fill-color: #e8eef5; }
        .navbar .back {
            padding: 8px 18px;
            background: transparent;
            color: #b08aff;
            border: 1px solid rgba(138, 90, 255, 0.3);
            border-radius: 20px;
            font-weight: 700;
            font-size: 12px;
            text-decoration: none;
            transition: 0.2s;
        }
        .navbar .back:hover { background: rgba(138, 90, 255, 0.1); }

        /* MAIN CONTAINER */
        .container {
            position: relative;
            z-index: 10;
            max-width: 900px;
            margin: 40px auto;
            padding: 0 24px;
            flex-grow: 1;
            width: 100%;
        }
        .header-title {
            text-align: center;
            margin-bottom: 30px;
        }
        .header-title h1 {
            font-size: 32px;
            font-weight: 900;
            background: linear-gradient(135deg, #b08aff, #ff8a5a);
            -webkit-background-clip: text;
            background-clip: text;
            -webkit-text-fill-color: transparent;
        }
        .header-title p { color: #8a7aaa; font-size: 14px; margin-top: 8px; }

        /* GRID LAYOUT FOR FORM AND RESULT */
        .app-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 24px;
        }
        @media (max-width: 768px) {
            .app-grid { grid-template-columns: 1fr; }
        }

        /* CARD STYLE */
        .glass-card {
            background: rgba(20, 10, 35, 0.5);
            border: 1px solid rgba(138, 90, 255, 0.15);
            border-radius: 18px;
            padding: 24px;
            backdrop-filter: blur(20px);
        }
        .glass-card h3 { font-size: 16px; margin-bottom: 20px; color: #b08aff; border-bottom: 1px solid rgba(138, 90, 255, 0.2); padding-bottom: 10px; }

        /* FORM ELEMENTS */
        .form-group { margin-bottom: 16px; }
        .form-group label { display: block; font-size: 12px; color: #8a7aaa; margin-bottom: 8px; font-weight: 600; text-transform: uppercase; letter-spacing: 1px; }
        .form-group input, .form-group textarea {
            width: 100%;
            padding: 12px 16px;
            background: rgba(0, 0, 0, 0.3);
            border: 1px solid rgba(138, 90, 255, 0.3);
            border-radius: 12px;
            color: #fff;
            font-family: inherit;
            font-size: 14px;
            transition: 0.3s;
            outline: none;
        }
        .form-group textarea { resize: vertical; min-height: 100px; }
        .form-group input:focus, .form-group textarea:focus { border-color: #ff8a5a; box-shadow: 0 0 15px rgba(255, 138, 90, 0.2); }
        
        /* STYLE UNTUK INPUT FILE */
        input[type="file"] {
            padding: 10px;
            color: #8a7aaa;
        }
        input[type="file"]::-webkit-file-upload-button {
            background: rgba(138, 90, 255, 0.2);
            border: 1px solid rgba(138, 90, 255, 0.5);
            color: #b08aff;
            padding: 8px 16px;
            border-radius: 8px;
            font-weight: 700;
            cursor: pointer;
            margin-right: 12px;
            transition: 0.3s;
        }
        input[type="file"]::-webkit-file-upload-button:hover {
            background: rgba(138, 90, 255, 0.4);
            color: #fff;
        }
        
        .btn-submit {
            width: 100%;
            padding: 14px;
            background: linear-gradient(135deg, #8a5aff, #ff8a5a);
            border: none;
            border-radius: 12px;
            color: #fff;
            font-weight: 800;
            font-size: 14px;
            cursor: pointer;
            transition: 0.3s;
            text-transform: uppercase;
            letter-spacing: 1px;
            box-shadow: 0 5px 20px rgba(138, 90, 255, 0.3);
            margin-top: 10px;
        }
        .btn-submit:hover { transform: translateY(-2px); box-shadow: 0 8px 25px rgba(255, 138, 90, 0.4); }
        .btn-submit:disabled { opacity: 0.6; cursor: not-allowed; transform: none; }

        /* RESULT AREA */
        .result-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 300px;
            text-align: center;
            border: 1px dashed rgba(138, 90, 255, 0.3);
            border-radius: 12px;
            background: rgba(0, 0, 0, 0.2);
            padding: 20px;
            overflow: hidden;
            position: relative;
        }
        .result-img {
            max-width: 100%;
            border-radius: 12px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.5);
            display: none;
        }
        .loader {
            display: none;
            border: 3px solid rgba(138, 90, 255, 0.2);
            border-top: 3px solid #ff8a5a;
            border-radius: 50%;
            width: 40px;
            height: 40px;
            animation: spin 1s linear infinite;
        }
        @keyframes spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
        
        .placeholder-text { color: #6a5a7a; font-size: 13px; }
        
        .btn-download {
            display: none;
            margin-top: 20px;
            padding: 10px 24px;
            background: rgba(138, 90, 255, 0.1);
            border: 1px solid #b08aff;
            border-radius: 20px;
            color: #b08aff;
            text-decoration: none;
            font-size: 12px;
            font-weight: 700;
            transition: 0.3s;
        }
        .btn-download:hover { background: #b08aff; color: #050308; }

        /* FOOTER */
        .footer {
            text-align: center;
            padding: 30px 24px;
            color: #3a2a4a;
            font-size: 11px;
            letter-spacing: 1.5px;
            z-index: 10;
            border-top: 1px solid rgba(138, 90, 255, 0.1);
            margin-top: 40px;
        }
    </style>
</head>
<body>

<canvas id="particles"></canvas>
<div class="glow-orb orb-1"></div>
<div class="glow-orb orb-2"></div>

<nav class="navbar">
    <a href="?go=tools" class="logo">XAERI<span>SOFT</span></a>
    <a href="?go=tools" class="back">← Kembali ke Tools</a>
</nav>

<div class="container">
    <div class="header-title">
        <h1>TikTok Quote Generator</h1>
        <p>Buat quotes ala komentar TikTok dengan mengunggah fotomu sendiri.</p>
    </div>

    <div class="app-grid">
        <!-- FORM PENGISIAN -->
        <div class="glass-card">
            <h3>📝 Pengaturan Quote</h3>
            <div class="form-group">
                <label>Nama Pengguna</label>
                <input type="text" id="quoteName" placeholder="Contoh: Xaerisoft" required>
            </div>
            
            <div class="form-group">
                <label>Teks Quote</label>
                <textarea id="quoteText" placeholder="Ketik quote / komentar kamu di sini..." required></textarea>
            </div>

            <div class="form-group">
                <label>Upload Foto Profil (Maks 5MB)</label>
                <!-- DIUBAH MENJADI INPUT FILE -->
                <input type="file" id="quoteFile" accept="image/*">
                <div style="font-size: 10px; color: #6a5a7a; margin-top: 6px;">*Boleh dikosongkan jika ingin memakai foto default.</div>
            </div>

            <button class="btn-submit" id="btnGenerate" onclick="generateQuote()">Generate Quote</button>
        </div>

        <!-- HASIL GENERATE -->
        <div class="glass-card">
            <h3>🖼️ Hasil Gambar</h3>
            <div class="result-container">
                <div class="placeholder-text" id="placeholder">Isi form di samping lalu klik Generate.</div>
                <div class="loader" id="loader"></div>
                <img id="resultImg" class="result-img" alt="TikTok Quote Result" crossorigin="anonymous">
            </div>
            <div style="text-align: center;">
                <a href="#" class="btn-download" id="btnDownload" target="_blank">Lihat Full Gambar / Simpan</a>
            </div>
        </div>
    </div>
</div>

<footer class="footer">
    <div>◆ XAERISOFT · 2026 ◆</div>
</footer>

<script>
    // ANIMASI BACKGROUND PARTICLES (Tema Xaerisoft)
    const canvas = document.getElementById('particles');
    const ctx = canvas.getContext('2d');
    function resize() { canvas.width = window.innerWidth; canvas.height = window.innerHeight; }
    resize(); window.addEventListener('resize', resize);

    const particles = [];
    for (let i = 0; i < 40; i++) {
        particles.push({
            x: Math.random() * canvas.width, y: Math.random() * canvas.height,
            size: Math.random() * 2 + 0.5, speedX: (Math.random() - 0.5) * 0.5, speedY: (Math.random() - 0.5) * 0.5,
            opacity: Math.random() * 0.5 + 0.1, color: Math.random() > 0.5 ? '#8a5aff' : '#ff8a5a'
        });
    }

    function animate() {
        ctx.clearRect(0, 0, canvas.width, canvas.height);
        particles.forEach(p => {
            p.x += p.speedX; p.y += p.speedY;
            if (p.x < 0) p.x = canvas.width; if (p.x > canvas.width) p.x = 0;
            if (p.y < 0) p.y = canvas.height; if (p.y > canvas.height) p.y = 0;
            ctx.beginPath(); ctx.arc(p.x, p.y, p.size, 0, Math.PI * 2);
            ctx.fillStyle = p.color; ctx.globalAlpha = p.opacity; ctx.fill();
        });
        ctx.globalAlpha = 1;
        requestAnimationFrame(animate);
    }
    animate();

    // LOGIKA GENERATOR QUOTE TIKTOK DENGAN UPLOAD FILE
    async function generateQuote() {
        const name = document.getElementById('quoteName').value.trim();
        const text = document.getElementById('quoteText').value.trim();
        const fileInput = document.getElementById('quoteFile');

        if (!name || !text) {
            alert("Harap isi Nama dan Teks Quote!");
            return;
        }

        // Referensi UI
        const btn = document.getElementById('btnGenerate');
        const loader = document.getElementById('loader');
        const placeholder = document.getElementById('placeholder');
        const resultImg = document.getElementById('resultImg');
        const btnDownload = document.getElementById('btnDownload');

        // Loading state
        btn.disabled = true;
        btn.innerText = "Mempersiapkan...";
        placeholder.style.display = "none";
        resultImg.style.display = "none";
        btnDownload.style.display = "none";
        loader.style.display = "block";

        // Foto Default jika user tidak upload gambar
        let finalImageUrl = "https://clooud.my.id/uploder/uploads/fRkHHO.jpeg";

        // JIKA USER MENGUPLOAD FILE
        if (fileInput.files.length > 0) {
            btn.innerText = "Mengunggah Foto...";
            const file = fileInput.files[0];
            const formData = new FormData();
            formData.append("file", file);

            try {
                // Upload diam-diam ke server telegra.ph untuk mendapatkan URL publik
                const uploadResponse = await fetch("https://telegra.ph/upload", {
                    method: "POST",
                    body: formData
                });
                
                const uploadData = await uploadResponse.json();
                
                if (uploadData && uploadData[0] && uploadData[0].src) {
                    finalImageUrl = "https://telegra.ph" + uploadData[0].src;
                } else {
                    throw new Error("Gagal mengunggah foto.");
                }
            } catch (error) {
                console.error(error);
                alert("Terjadi kesalahan saat upload foto, foto akan otomatis diubah ke default.");
            }
        }

        btn.innerText = "Membuat Quote...";

        // Susun URL API Endpoint dengan query parameter yang sudah mendapat URL Gambar Publik
        const apiUrl = `https://apii.nexadev.my.id/ttqc?url=${encodeURIComponent(finalImageUrl)}&name=${encodeURIComponent(name)}&text=${encodeURIComponent(text)}`;

        // Eksekusi pemanggilan gambar
        resultImg.onload = function() {
            loader.style.display = "none";
            resultImg.style.display = "block";
            btnDownload.style.display = "inline-block";
            btnDownload.href = apiUrl; 
            
            btn.disabled = false;
            btn.innerText = "Generate Ulang";
        };

        resultImg.onerror = function() {
            loader.style.display = "none";
            placeholder.style.display = "block";
            placeholder.innerHTML = "<span style='color:#ff5a8a'>Gagal membuat quote. Coba ubah teks atau foto lalu ulangi.</span>";
            
            btn.disabled = false;
            btn.innerText = "Coba Lagi";
        };

        // Trigger load image API
        resultImg.src = apiUrl;
    }
</script>

</body>
</html>
