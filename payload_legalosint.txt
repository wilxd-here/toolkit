<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Xaerisoft · OSINT V3</title>
    <style>
        * { margin:0; padding:0; box-sizing:border-box; }
        body { background:#050308; color:#e8eef5; font-family:-apple-system,'Segoe UI',system-ui,sans-serif; min-height:100vh; overflow-x:hidden; }
        .navbar { position:sticky; top:0; z-index:100; padding:16px 28px; display:flex; justify-content:space-between; align-items:center; border-bottom:1px solid rgba(138,90,255,0.15); background:rgba(5,3,8,0.9); backdrop-filter:blur(20px); }
        .navbar .logo { font-size:20px; font-weight:800; background:linear-gradient(135deg,#b08aff,#ff8a5a); -webkit-background-clip:text; background-clip:text; -webkit-text-fill-color:transparent; letter-spacing:1px; text-decoration:none; }
        .navbar .logo span { color:#e8eef5; -webkit-text-fill-color:#e8eef5; }
        .navbar .menu { display:flex; gap:20px; align-items:center; }
        .navbar .menu a { color:#6a5a7a; text-decoration:none; font-size:13px; font-weight:600; transition:0.2s; }
        .navbar .menu a:hover { color:#b08aff; }
        .navbar .btn-tools { padding:8px 18px; background:linear-gradient(135deg,#8a5aff,#b08aff); color:#fff !important; border-radius:20px; font-weight:700; font-size:12px; box-shadow:0 0 20px rgba(138,90,255,0.3); }
        .hero { max-width:1000px; margin:0 auto; padding:50px 24px 30px; text-align:center; }
        .hero .badge { display:inline-block; padding:6px 16px; background:rgba(138,90,255,0.15); border:1px solid rgba(138,90,255,0.3); border-radius:20px; color:#b08aff; font-size:11px; font-weight:700; letter-spacing:1.5px; text-transform:uppercase; margin-bottom:20px; }
        .hero h1 { font-size:42px; font-weight:900; margin-bottom:14px; background:linear-gradient(135deg,#b08aff,#ff8a5a); background-size:200% auto; -webkit-background-clip:text; background-clip:text; -webkit-text-fill-color:transparent; animation:shine 4s linear infinite; }
        @keyframes shine { 0% { background-position:0% center; } 100% { background-position:200% center; } }
        .hero p { color:#8a7aaa; font-size:15px; line-height:1.7; max-width:550px; margin:0 auto; }
        .tabs { max-width:1000px; margin:30px auto 0; padding:0 24px; display:flex; gap:4px; background:rgba(20,10,35,0.5); padding:6px; border-radius:14px; border:1px solid rgba(138,90,255,0.15); flex-wrap:wrap; }
        .tab-btn { flex:1; min-width:70px; padding:8px 10px; background:transparent; color:#6a5a7a; border:none; border-radius:10px; font-size:10px; font-weight:700; cursor:pointer; transition:0.2s; text-transform:uppercase; font-family:inherit; }
        .tab-btn.active { background:linear-gradient(135deg,#8a5aff,#b08aff); color:#fff; box-shadow:0 0 20px rgba(138,90,255,0.3); }
        .tab-btn:hover:not(.active) { color:#b08aff; }
        .section { max-width:1000px; margin:0 auto; padding:30px 24px 60px; }
        .panel { display:none; }
        .panel.active { display:block; }
        .card { background:rgba(20,10,35,0.5); border:1px solid rgba(138,90,255,0.15); border-radius:18px; padding:24px; backdrop-filter:blur(20px); margin-bottom:16px; }
        .card h2 { font-size:16px; font-weight:700; margin-bottom:6px; }
        .card p { color:#8a7aaa; font-size:12px; margin-bottom:16px; line-height:1.6; }
        .input-group { display:flex; gap:8px; margin-bottom:14px; flex-wrap:wrap; }
        .input-group input, .input-group select, .input-group textarea { flex:1; padding:12px 14px; background:rgba(8,8,18,0.8); border:1px solid rgba(138,90,255,0.15); border-radius:10px; color:#fff; font-size:13px; outline:none; min-width:150px; font-family:inherit; transition:0.2s; }
        .input-group input:focus, .input-group select:focus, .input-group textarea:focus { border-color:#8a5aff; box-shadow:0 0 0 3px rgba(138,90,255,0.1); }
        .input-group input::placeholder, .input-group textarea::placeholder { color:#4a3a5a; }
        .input-group textarea { min-height:80px; resize:vertical; }
        .btn { padding:12px 20px; background:linear-gradient(135deg,#8a5aff,#b08aff); color:#fff; border:none; border-radius:10px; font-weight:700; font-size:13px; cursor:pointer; transition:0.2s; font-family:inherit; white-space:nowrap; }
        .btn:hover { transform:translateY(-2px); box-shadow:0 10px 30px rgba(138,90,255,0.4); }
        .btn:disabled { opacity:0.5; cursor:not-allowed; }
        .btn-sm { padding:8px 14px; font-size:12px; }
        .result { background:rgba(8,8,18,0.8); border:1px solid rgba(138,90,255,0.15); border-radius:10px; padding:16px; font-family:'Courier New',monospace; font-size:11px; line-height:1.8; color:#b08aff; max-height:500px; overflow-y:auto; white-space:pre-wrap; word-break:break-all; }
        .result .success { color:#4aff9e; }
        .result .error { color:#ff5577; }
        .result .warning { color:#ffcc55; }
        .result .info { color:#00ccff; }
        .result .label { color:#ff8a5a; font-weight:700; }
        .result a { color:#00ccff; text-decoration:none; }
        .result a:hover { text-decoration:underline; }
        .grid-2 { display:grid; grid-template-columns:repeat(auto-fit,minmax(200px,1fr)); gap:12px; }
        .mini-card { background:rgba(8,8,18,0.6); border:1px solid rgba(138,90,255,0.1); border-radius:10px; padding:14px; }
        .mini-card h3 { font-size:13px; margin-bottom:6px; color:#b08aff; }
        .mini-card p { font-size:11px; color:#6a5a7a; margin-bottom:10px; }
        .footer { text-align:center; padding:40px 24px; color:#3a2a4a; font-size:11px; letter-spacing:1.5px; text-transform:uppercase; border-top:1px solid rgba(138,90,255,0.1); }
        .footer span { color:#8a5aff; }
        @media (max-width:600px) { .hero h1 { font-size:28px; } .navbar { padding:12px 16px; } .navbar .logo { font-size:16px; } .navbar .menu { gap:10px; } .navbar .menu a { font-size:11px; } .card { padding:18px; } .tabs { padding:0 16px; margin-top:20px; } .section { padding:20px 16px 40px; } }
    </style>
</head>
<body>

<nav class="navbar">
    <a href="?" class="logo">XAERI<span>SOFT</span></a>
    <div class="menu">
        <a href="?go=tools">Tools</a>
        <a href="?">Home</a>
        <a href="?go=tools" class="btn-tools">🚀 Buka Tools</a>
    </div>
</nav>

<section class="hero">
    <div class="badge">🔥 OSINT V3</div>
    <h1>OSINT Ultimate V3</h1>
    <p>40+ Tools OSINT tanpa API. Cari username, email, domain, IP, nomor, foto, hash, dan lainnya. 100% Legal & Gratis.</p>
</section>

<div class="tabs">
    <button class="tab-btn active" data-tab="username">👤 User</button>
    <button class="tab-btn" data-tab="email">📧 Email</button>
    <button class="tab-btn" data-tab="domain">🌐 Domain</button>
    <button class="tab-btn" data-tab="ip">📡 IP</button>
    <button class="tab-btn" data-tab="phone">📱 Nomor</button>
    <button class="tab-btn" data-tab="web">🕸️ Web</button>
    <button class="tab-btn" data-tab="crypto">🔐 Crypto</button>
    <button class="tab-btn" data-tab="image">🖼️ Foto</button>
    <button class="tab-btn" data-tab="tools">🛠️ Tools</button>
    <button class="tab-btn" data-tab="util">⚙️ Utility</button>
</div>

<section class="section">

    <!-- USERNAME -->
    <div class="panel active" id="panel-username">
        <div class="card">
            <h2>👤 Cek Username (50+ Platform)</h2>
            <p>Generate link otomatis ke 50+ platform publik</p>
            <div class="input-group">
                <input type="text" id="usernameInput" placeholder="contoh: johndoe" />
                <button class="btn" id="checkUsername">🔍 Cek</button>
            </div>
            <div class="result" id="resultUsername">Hasil bakal muncul di sini...</div>
        </div>
    </div>

    <!-- EMAIL -->
    <div class="panel" id="panel-email">
        <div class="card">
            <h2>📧 Cek Email</h2>
            <p>Cek data breach, gravatar, sosmed terkait</p>
            <div class="input-group">
                <input type="text" id="emailInput" placeholder="contoh: user@example.com" />
                <button class="btn" id="checkEmail">🔍 Cek</button>
            </div>
            <div class="result" id="resultEmail">Hasil bakal muncul di sini...</div>
        </div>
        <div class="card">
            <h2>✅ Email Validator</h2>
            <p>Cek format & MX record email</p>
            <div class="input-group">
                <input type="text" id="emailValidateInput" placeholder="contoh: user@example.com" />
                <button class="btn" id="validateEmail">🔍 Cek</button>
            </div>
            <div class="result" id="resultEmailValidate">Hasil bakal muncul di sini...</div>
        </div>
    </div>

    <!-- DOMAIN -->
    <div class="panel" id="panel-domain">
        <div class="card">
            <h2>🌐 Cek Domain</h2>
            <p>DNS lookup (7 record types)</p>
            <div class="input-group">
                <input type="text" id="domainInput" placeholder="contoh: example.com" />
                <button class="btn" id="checkDomain">🔍 Cek</button>
            </div>
            <div class="result" id="resultDomain">Hasil bakal muncul di sini...</div>
        </div>
        <div class="card">
            <h2>🔍 DNS Propagation</h2>
            <p>Cek DNS di berbagai server</p>
            <div class="input-group">
                <input type="text" id="propagationInput" placeholder="contoh: example.com" />
                <button class="btn" id="checkPropagation">🔍 Cek</button>
            </div>
            <div class="result" id="resultPropagation">Hasil bakal muncul di sini...</div>
        </div>
    </div>

    <!-- IP -->
    <div class="panel" id="panel-ip">
        <div class="card">
            <h2>📡 Cek IP Address</h2>
            <p>Info IP, geolocation, ISP</p>
            <div class="input-group">
                <input type="text" id="ipInput" placeholder="contoh: 8.8.8.8" />
                <button class="btn" id="checkIP">🔍 Cek</button>
            </div>
            <div class="result" id="resultIP">Hasil bakal muncul di sini...</div>
        </div>
        <div class="card">
            <h2>🧮 IP Range Calculator</h2>
            <p>Hitung subnet dari CIDR</p>
            <div class="input-group">
                <input type="text" id="cidrInput" placeholder="contoh: 192.168.1.0/24" />
                <button class="btn" id="calcCIDR">🔍 Hitung</button>
            </div>
            <div class="result" id="resultCIDR">Hasil bakal muncul di sini...</div>
        </div>
    </div>

    <!-- PHONE -->
    <div class="panel" id="panel-phone">
        <div class="card">
            <h2>📱 Cek Nomor HP</h2>
            <p>Provider, link publik</p>
            <div class="input-group">
                <input type="text" id="phoneInput" placeholder="contoh: 628123456789" />
                <button class="btn" id="checkPhone">🔍 Cek</button>
            </div>
            <div class="result" id="resultPhone">Hasil bakal muncul di sini...</div>
        </div>
    </div>

    <!-- WEB -->
    <div class="panel" id="panel-web">
        <div class="card">
            <h2>🕸️ Web Analysis</h2>
            <p>Cek robots.txt, sitemap, HTTP status</p>
            <div class="input-group">
                <input type="text" id="webInput" placeholder="contoh: example.com" />
                <button class="btn" id="checkWeb">🔍 Cek</button>
            </div>
            <div class="result" id="resultWeb">Hasil bakal muncul di sini...</div>
        </div>
        <div class="card">
            <h2>🔄 HTTP Status & Redirect</h2>
            <p>Cek status code & redirect</p>
            <div class="input-group">
                <input type="text" id="httpInput" placeholder="contoh: https://example.com" />
                <button class="btn" id="checkHTTP">🔍 Cek</button>
            </div>
            <div class="result" id="resultHTTP">Hasil bakal muncul di sini...</div>
        </div>
    </div>

    <!-- CRYPTO -->
    <div class="panel" id="panel-crypto">
        <div class="card">
            <h2>🔐 Hash Generator</h2>
            <p>Generate MD5, SHA1, SHA256, SHA512</p>
            <div class="input-group">
                <input type="text" id="hashInput" placeholder="Masukkan teks..." />
                <button class="btn" id="generateHash">🔍 Generate</button>
            </div>
            <div class="result" id="resultHash">Hasil bakal muncul di sini...</div>
        </div>
        <div class="card">
            <h2>🔓 JWT Decoder</h2>
            <p>Decode JWT token (header + payload)</p>
            <div class="input-group">
                <input type="text" id="jwtInput" placeholder="Paste JWT token..." />
                <button class="btn" id="decodeJWT">🔍 Decode</button>
            </div>
            <div class="result" id="resultJWT">Hasil bakal muncul di sini...</div>
        </div>
        <div class="card">
            <h2>🔑 Password Strength</h2>
            <p>Cek kekuatan password</p>
            <div class="input-group">
                <input type="text" id="passInput" placeholder="Masukkan password..." />
                <button class="btn" id="checkPassword">🔍 Cek</button>
            </div>
            <div class="result" id="resultPassword">Hasil bakal muncul di sini...</div>
        </div>
    </div>

    <!-- IMAGE -->
    <div class="panel" id="panel-image">
        <div class="card">
            <h2>🖼️ Reverse Image Search</h2>
            <p>Cari sumber gambar dari URL</p>
            <div class="input-group">
                <input type="text" id="imageInput" placeholder="contoh: https://example.com/gambar.jpg" />
                <button class="btn" id="checkImage">🔍 Cek</button>
            </div>
            <div class="result" id="resultImage">Hasil bakal muncul di sini...</div>
        </div>
        <div class="card">
            <h2>🎨 QR Code Generator</h2>
            <p>Generate QR code dari teks</p>
            <div class="input-group">
                <input type="text" id="qrInput" placeholder="Masukkan teks/URL..." />
                <button class="btn" id="generateQR">🔍 Generate</button>
            </div>
            <div class="result" id="resultQR">Hasil bakal muncul di sini...</div>
        </div>
    </div>

    <!-- TOOLS -->
    <div class="panel" id="panel-tools">
        <div class="card">
            <h2>🛠️ 40+ Tools OSINT Terbaik</h2>
            <p>Kumpulan situs OSINT gratis & legal</p>
            <div class="grid-2">
                <div class="mini-card">
                    <h3>📧 Email & Breach</h3>
                    <p>Cek data breach</p>
                    <a href="https://haveibeenpwned.com" target="_blank" class="btn btn-sm">HIBP →</a>
                    <a href="https://epieos.com" target="_blank" class="btn btn-sm">Epieos →</a>
                    <a href="https://intelx.io" target="_blank" class="btn btn-sm">IntelX →</a>
                </div>
                <div class="mini-card">
                    <h3>👤 Username</h3>
                    <p>Cari username</p>
                    <a href="https://whatsmyname.app" target="_blank" class="btn btn-sm">WhatsMyName →</a>
                    <a href="https://namechk.com" target="_blank" class="btn btn-sm">Namechk →</a>
                </div>
                <div class="mini-card">
                    <h3>🌐 Domain & IP</h3>
                    <p>Recon domain</p>
                    <a href="https://viewdns.info" target="_blank" class="btn btn-sm">ViewDNS →</a>
                    <a href="https://dnsdumpster.com" target="_blank" class="btn btn-sm">DNSDumpster →</a>
                    <a href="https://crt.sh" target="_blank" class="btn btn-sm">crt.sh →</a>
                </div>
                <div class="mini-card">
                    <h3>📡 Server</h3>
                    <p>Cek server</p>
                    <a href="https://shodan.io" target="_blank" class="btn btn-sm">Shodan →</a>
                    <a href="https://censys.io" target="_blank" class="btn btn-sm">Censys →</a>
                    <a href="https://urlscan.io" target="_blank" class="btn btn-sm">URLScan →</a>
                </div>
                <div class="mini-card">
                    <h3>📱 Phone</h3>
                    <p>OSINT phone</p>
                    <a href="https://phoneinfoga.crvx.fr" target="_blank" class="btn btn-sm">PhoneInfoga →</a>
                    <a href="https://truecaller.com" target="_blank" class="btn btn-sm">Truecaller →</a>
                </div>
                <div class="mini-card">
                    <h3>🖼️ Image</h3>
                    <p>Reverse image</p>
                    <a href="https://lens.google.com" target="_blank" class="btn btn-sm">Google Lens →</a>
                    <a href="https://yandex.com/images" target="_blank" class="btn btn-sm">Yandex →</a>
                    <a href="https://tineye.com" target="_blank" class="btn btn-sm">TinEye →</a>
                </div>
                <div class="mini-card">
                    <h3>📚 Archive</h3>
                    <p>Web archive</p>
                    <a href="https://web.archive.org" target="_blank" class="btn btn-sm">Wayback →</a>
                    <a href="https://archive.ph" target="_blank" class="btn btn-sm">Archive.today →</a>
                </div>
                <div class="mini-card">
                    <h3>🔐 Security</h3>
                    <p>Security check</p>
                    <a href="https://virustotal.com" target="_blank" class="btn btn-sm">VirusTotal →</a>
                    <a href="https://abuseipdb.com" target="_blank" class="btn btn-sm">AbuseIPDB →</a>
                </div>
            </div>
        </div>
    </div>

      <!-- UTILITY -->
    <div class="panel" id="panel-util">
        <div class="card">
            <h2>🔤 Base64 Encode/Decode</h2>
            <p>Encode/decode base64</p>
            <div class="input-group">
                <input type="text" id="base64Input" placeholder="Masukkan teks..." />
                <button class="btn" id="encodeBase64">🔒 Encode</button>
                <button class="btn" id="decodeBase64">🔓 Decode</button>
            </div>
            <div class="result" id="resultBase64">Hasil bakal muncul di sini...</div>
        </div>
        <div class="card">
            <h2>🔗 URL Encode/Decode</h2>
            <p>Encode/decode URL</p>
            <div class="input-group">
                <input type="text" id="urlInput2" placeholder="Masukkan URL..." />
                <button class="btn" id="encodeURL">🔒 Encode</button>
                <button class="btn" id="decodeURL">🔓 Decode</button>
            </div>
            <div class="result" id="resultURL">Hasil bakal muncul di sini...</div>
        </div>
        <div class="card">
            <h2>🎲 UUID Generator</h2>
            <p>Generate UUID v4 random</p>
            <div class="input-group">
                <button class="btn" id="generateUUID">🔍 Generate</button>
            </div>
            <div class="result" id="resultUUID">Hasil bakal muncul di sini...</div>
        </div>
        <div class="card">
            <h2>📝 Lorem Ipsum</h2>
            <p>Generate text dummy</p>
            <div class="input-group">
                <input type="number" id="loremCount" placeholder="Jumlah paragraf" value="3" />
                <button class="btn" id="generateLorem">🔍 Generate</button>
            </div>
            <div class="result" id="resultLorem">Hasil bakal muncul di sini...</div>
        </div>
        <div class="card">
            <h2>🔤 Text Case Converter</h2>
            <p>Ubah case teks</p>
            <div class="input-group">
                <input type="text" id="caseInput" placeholder="Masukkan teks..." />
                <button class="btn" id="toUpper">UPPER</button>
                <button class="btn" id="toLower">lower</button>
                <button class="btn" id="toTitle">Title</button>
                <button class="btn" id="toReverse">Reverse</button>
            </div>
            <div class="result" id="resultCase">Hasil bakal muncul di sini...</div>
        </div>
        <div class="card">
            <h2>📋 Duplicate Remover</h2>
            <p>Hapus baris duplikat</p>
            <div class="input-group">
                <textarea id="dupInput" placeholder="Paste teks (1 baris 1 item)..."></textarea>
            </div>
            <div class="input-group">
                <button class="btn" id="removeDup">🔍 Hapus Duplikat</button>
            </div>
            <div class="result" id="resultDup">Hasil bakal muncul di sini...</div>
        </div>
    </div>

</section>

<footer class="footer">
    <div><span>◆</span> XAERISOFT · 2026 · OSINT V3 <span>◆</span></div>
</footer>

<script>
    // TABS
    document.querySelectorAll('.tab-btn').forEach(btn => {
        btn.addEventListener('click', () => {
            document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
            document.querySelectorAll('.panel').forEach(p => p.classList.remove('active'));
            btn.classList.add('active');
            document.getElementById('panel-' + btn.dataset.tab).classList.add('active');
        });
    });

    // PLATFORMS
    const PLATFORMS = [
        { name:'GitHub', url:'https://github.com/{}' },
        { name:'Instagram', url:'https://instagram.com/{}' },
        { name:'Twitter/X', url:'https://twitter.com/{}' },
        { name:'TikTok', url:'https://tiktok.com/@{}' },
        { name:'Facebook', url:'https://facebook.com/{}' },
        { name:'Reddit', url:'https://reddit.com/user/{}' },
        { name:'YouTube', url:'https://youtube.com/@{}' },
        { name:'Pinterest', url:'https://pinterest.com/{}' },
        { name:'Telegram', url:'https://t.me/{}' },
        { name:'Medium', url:'https://medium.com/@{}' },
        { name:'Dev.to', url:'https://dev.to/{}' },
        { name:'GitLab', url:'https://gitlab.com/{}' },
        { name:'Bitbucket', url:'https://bitbucket.org/{}' },
        { name:'Twitch', url:'https://twitch.tv/{}' },
        { name:'Steam', url:'https://steamcommunity.com/id/{}' },
        { name:'Spotify', url:'https://open.spotify.com/user/{}' },
        { name:'SoundCloud', url:'https://soundcloud.com/{}' },
        { name:'Vimeo', url:'https://vimeo.com/{}' },
        { name:'Flickr', url:'https://flickr.com/people/{}' },
        { name:'Tumblr', url:'https://{}.tumblr.com' },
        { name:'WordPress', url:'https://{}.wordpress.com' },
        { name:'About.me', url:'https://about.me/{}' },
        { name:'Behance', url:'https://behance.net/{}' },
        { name:'Dribbble', url:'https://dribbble.com/{}' },
        { name:'Kaggle', url:'https://kaggle.com/{}' },
        { name:'Keybase', url:'https://keybase.io/{}' },
        { name:'Mastodon', url:'https://mastodon.social/@{}' },
        { name:'Snapchat', url:'https://snapchat.com/add/{}' },
        { name:'AskFM', url:'https://ask.fm/{}' },
        { name:'Badoo', url:'https://badoo.com/en/{}' },
        { name:'Foursquare', url:'https://foursquare.com/{}' },
        { name:'Goodreads', url:'https://goodreads.com/{}' },
        { name:'Last.fm', url:'https://last.fm/user/{}' },
        { name:'Mixcloud', url:'https://mixcloud.com/{}' },
        { name:'Patreon', url:'https://patreon.com/{}' },
        { name:'ProductHunt', url:'https://producthunt.com/@{}' },
        { name:'Quora', url:'https://quora.com/profile/{}' },
        { name:'SlideShare', url:'https://slideshare.net/{}' },
        { name:'Trello', url:'https://trello.com/{}' },
        { name:'VK', url:'https://vk.com/{}' },
        { name:'Wattpad', url:'https://wattpad.com/user/{}' },
        { name:'9GAG', url:'https://9gag.com/u/{}' },
        { name:'Zhihu', url:'https://zhihu.com/people/{}' },
        { name:'Line', url:'https://line.me/ti/p/~{}' },
        { name:'Vero', url:'https://vero.co/{}' },
    ];

    // USERNAME
    document.getElementById('checkUsername').addEventListener('click', () => {
        const u = document.getElementById('usernameInput').value.trim();
        const r = document.getElementById('resultUsername');
        if (!u) { alert('Masukkan username!'); return; }
        let html = `<span class="label">🔍 MENCARI: ${u}</span>\n──────────────────────\n\n`;
        PLATFORMS.forEach(p => {
            const url = p.url.replace('{}', u);
            html += `<span class="success">✅ ${p.name.padEnd(18)}</span> → <a href="${url}" target="_blank">${url}</a>\n`;
        });
        html += `\n──────────────────────\n<span class="info">📊 TOTAL: ${PLATFORMS.length} platform</span>`;
        r.innerHTML = html;
    });

    // EMAIL
    document.getElementById('checkEmail').addEventListener('click', async () => {
        const e = document.getElementById('emailInput').value.trim();
        const r = document.getElementById('resultEmail');
        if (!e || !e.includes('@')) { alert('Masukkan email valid!'); return; }
        r.innerHTML = '<span class="warning">⏳ Cek...</span>';
        let html = `<span class="label">📧 CEK: ${e}</span>\n──────────────────────\n\n`;
        try {
            const proxy = 'https://api.allorigins.win/raw?url=';
            const api = `https://haveibeenpwned.com/api/v3/breachedaccount/${encodeURIComponent(e)}?truncateResponse=false`;
            const res = await fetch(proxy + encodeURIComponent(api));
            if (res.status === 200) {
                const b = await res.json();
                html += `<span class="error">⚠️ KENA ${b.length} BREACH!</span>\n\n`;
                b.forEach(x => html += `<span class="error">🔴 ${x.Name}</span> (${x.BreachDate})\n   ${x.DataClasses.join(', ')}\n\n`);
            } else if (res.status === 404) {
                html += `<span class="success">✅ Email AMAN</span>\n\n`;
            } else {
                html += `<span class="warning">⚠️ Gagal (${res.status})</span>\n\n`;
            }
        } catch (err) { html += `<span class="warning">⚠️ ${err.message}</span>\n\n`; }
        html += `<span class="info">🔗 Manual:</span>\n<a href="https://haveibeenpwned.com/account/${e}" target="_blank">🔗 HIBP</a>\n<a href="https://epieos.com/?q=${e}" target="_blank">🔗 Epieos</a>\n`;
        r.innerHTML = html;
    });

    // DOMAIN
    document.getElementById('checkDomain').addEventListener('click', async () => {
        const d = document.getElementById('domainInput').value.trim();
        const r = document.getElementById('resultDomain');
        if (!d) { alert('Masukkan domain!'); return; }
        r.innerHTML = '<span class="warning">⏳ Lookup...</span>';
        let html = `<span class="label">🌐 CEK: ${d}</span>\n──────────────────────\n\n`;
        try {
            for (const t of ['A','AAAA','MX','TXT','NS','CNAME','SOA']) {
                const res = await fetch(`https://cloudflare-dns.com/dns-query?name=${d}&type=${t}`, { headers:{'Accept':'application/dns-json'} });
                const data = await res.json();
                if (data.Answer) {
                    html += `<span class="info">📡 ${t}:</span>\n`;
                    data.Answer.forEach(a => html += `<span class="success">  ✅ ${a.name} → ${a.data} (TTL:${a.TTL})</span>\n`);
                    html += `\n`;
                }
            }
        } catch (err) { html += `<span class="error">❌ ${err.message}</span>\n\n`; }
        html += `<span class="info">🔗 Manual:</span>\n<a href="https://who.is/whois/${d}" target="_blank">🔗 WHOIS</a>\n<a href="https://crt.sh/?q=${d}" target="_blank">🔗 Subdomain</a>\n<a href="https://dnsdumpster.com" target="_blank">🔗 DNSDumpster</a>\n`;
        r.innerHTML = html;
    });

    // DNS PROPAGATION
    document.getElementById('checkPropagation').addEventListener('click', async () => {
        const d = document.getElementById('propagationInput').value.trim();
        const r = document.getElementById('resultPropagation');
        if (!d) { alert('Masukkan domain!'); return; }
        r.innerHTML = '<span class="warning">⏳ Cek DNS global...</span>';
        let html = `<span class="label">🔍 DNS PROPAGATION: ${d}</span>\n──────────────────────\n\n`;
        const servers = [
            { name:'Cloudflare', url:'https://cloudflare-dns.com/dns-query' },
            { name:'Google', url:'https://dns.google/resolve' },
        ];
        for (const s of servers) {
            try {
                const res = await fetch(`${s.url}?name=${d}&type=A`, { headers:{'Accept':'application/dns-json'} });
                const data = await res.json();
                if (data.Answer) {
                    html += `<span class="success">✅ ${s.name}:</span>\n`;
                    data.Answer.forEach(a => html += `   ${a.data}\n`);
                } else {
                    html += `<span class="warning">⚠️ ${s.name}: No data</span>\n`;
                }
            } catch (e) {
                html += `<span class="error">❌ ${s.name}: ${e.message}</span>\n`;
            }
        }
        r.innerHTML = html;
    });

    // IP
    document.getElementById('checkIP').addEventListener('click', async () => {
        const ip = document.getElementById('ipInput').value.trim();
        const r = document.getElementById('resultIP');
        if (!ip) { alert('Masukkan IP!'); return; }
        r.innerHTML = '<span class="warning">⏳ Cek IP...</span>';
        let html = `<span class="label">📡 CEK: ${ip}</span>\n──────────────────────\n\n`;
        try {
            const res = await fetch(`https://ipapi.co/${ip}/json/`);
            const data = await res.json();
            if (data.ip) {
                html += `<span class="success">🌐 IP       : ${data.ip}</span>\n`;
                html += `<span class="success">🏙️ Kota     : ${data.city||'-'}</span>\n`;
                html += `<span class="success">🗺️ Region   : ${data.region||'-'}</span>\n`;
                html += `<span class="success">🌍 Negara   : ${data.country_name||'-'}</span>\n`;
                html += `<span class="success">📡 ISP      : ${data.org||'-'}</span>\n`;
                html += `<span class="success">🕐 Timezone : ${data.timezone||'-'}</span>\n`;
                html += `<span class="success">📍 Lat/Long : ${data.latitude}, ${data.longitude}</span>\n\n`;
            } else html += `<span class="error">❌ IP tidak valid</span>\n\n`;
        } catch (e) { html += `<span class="error">❌ ${e.message}</span>\n\n`; }
        r.innerHTML = html;
    });

    // CIDR
    document.getElementById('calcCIDR').addEventListener('click', () => {
        const c = document.getElementById('cidrInput').value.trim();
        const r = document.getElementById('resultCIDR');
        if (!c.includes('/')) { alert('Format: 192.168.1.0/24'); return; }
        const [ip, mask] = c.split('/');
        const m = parseInt(mask);
        const parts = ip.split('.').map(Number);
        const ipNum = (parts[0]<<24) + (parts[1]<<16) + (parts[2]<<8) + parts[3];
        const maskNum = m === 0 ? 0 : (0xFFFFFFFF << (32 - m)) >>> 0;
        const network = (ipNum & maskNum) >>> 0;
        const broadcast = (network | (~maskNum >>> 0)) >>> 0;
        const total = Math.pow(2, 32 - m);
        const usable = total > 2 ? total - 2 : total;
        const toIP = n => [(n>>>24)&255,(n>>>16)&255,(n>>>8)&255,n&255].join('.');
        let html = `<span class="label">🧮 CIDR: ${c}</span>\n──────────────────────\n\n`;
        html += `<span class="success">📡 Network   : ${toIP(network)}</span>\n`;
        html += `<span class="success">📢 Broadcast : ${toIP(broadcast)}</span>\n`;
        html += `<span class="success">🎭 Netmask   : ${toIP(maskNum)}</span>\n`;
        html += `<span class="success">📊 Total IP  : ${total}</span>\n`;
        html += `<span class="success">✅ Usable IP : ${usable}</span>\n`;
        html += `<span class="success">🔢 First IP  : ${toIP(network+1)}</span>\n`;
        html += `<span class="success">🔢 Last IP   : ${toIP(broadcast-1)}</span>\n`;
        r.innerHTML = html;
    });

    // PHONE
    const PROVIDERS = [
        { name:'Telkomsel', prefixes:['0811','0812','0813','0814','0815','0816','0817','0818','0819','0821','0822','0823','0851','0852','0853','0854','0855','0856','0857','0858','0859'] },
        { name:'Indosat', prefixes:['0814','0815','0816','0855','0856','0857','0858','0859'] },
        { name:'XL', prefixes:['0817','0818','0819','0859','0877','0878','0879'] },
        { name:'Tri', prefixes:['0895','0896','0897','0898','0899'] },
        { name:'Smartfren', prefixes:['0881','0882','0883','0884','0885','0886','0887','0888','0889'] },
    ];
    function formatN(n){ n=n.replace(/[^0-9]/g,''); if(n.startsWith('0'))n='62'+n.slice(1); if(!n.startsWith('62'))n='62'+n; return n; }
    function getProvider(n){ for(const p of PROVIDERS){ for(const x of p.prefixes){ if(n.startsWith(x)) return p.name; } } return 'Tidak diketahui'; }
    document.getElementById('checkPhone').addEventListener('click', () => {
        let n = document.getElementById('phoneInput').value.trim();
        const r = document.getElementById('resultPhone');
        if (!n || n.length < 10) { alert('Masukkan nomor valid!'); return; }
        n = formatN(n);
        const p = getProvider(n);
        let html = `<span class="label">📱 CEK: ${n}</span>\n──────────────────────\n\n`;
        html += `<span class="success">📡 Provider : ${p}</span>\n`;
        html += `<span class="success">🌍 Negara   : Indonesia</span>\n`;
        html += `<span class="success">📏 Panjang  : ${n.length} digit</span>\n`;
        html += `<span class="success">✅ Valid    : ${n.length >= 11 && n.length <= 14 ? 'Ya' : 'Tidak'}</span>\n\n`;
        html += `<span class="info">🔗 Manual:</span>\n<a href="https://wa.me/${n}" target="_blank">💬 WhatsApp</a>\n<a href="https://truecaller.com/search/id/${n}" target="_blank">📞 Truecaller</a>\n`;
        r.innerHTML = html;
    });

    // WEB
    document.getElementById('checkWeb').addEventListener('click', async () => {
        const d = document.getElementById('webInput').value.trim();
        const r = document.getElementById('resultWeb');
        if (!d) { alert('Masukkan domain!'); return; }
        r.innerHTML = '<span class="warning">⏳ Analisis...</span>';
        let html = `<span class="label">🕸️ WEB: ${d}</span>\n──────────────────────\n\n`;
        try {
            const res = await fetch(`https://api.allorigins.win/raw?url=${encodeURIComponent('https://'+d+'/robots.txt')}`);
            if (res.ok) { const t = await res.text(); html += t.length > 0 && !t.includes('<!DOCTYPE') ? `<span class="success">✅ robots.txt ADA (${t.length} bytes)</span>\n` : `<span class="warning">⚠️ robots.txt kosong</span>\n`; }
        } catch(e) { html += `<span class="warning">⚠️ robots.txt error</span>\n`; }
        try {
            const res = await fetch(`https://api.allorigins.win/raw?url=${encodeURIComponent('https://'+d+'/sitemap.xml')}`);
            if (res.ok) { const t = await res.text(); html += (t.includes('<urlset')||t.includes('<sitemapindex')) ? `<span class="success">✅ sitemap.xml ADA</span>\n` : `<span class="warning">⚠️ sitemap kosong</span>\n`; }
        } catch(e) { html += `<span class="warning">⚠️ sitemap error</span>\n`; }
        html += `\n<span class="info">🔗 Manual:</span>\n<a href="https://securityheaders.com/?q=${d}" target="_blank">🔗 SecurityHeaders</a>\n<a href="https://www.ssllabs.com/ssltest/analyze.html?d=${d}" target="_blank">🔗 SSL Labs</a>\n<a href="https://builtwith.com/${d}" target="_blank">🔗 BuiltWith</a>\n`;
        r.innerHTML = html;
    });

    // HTTP
    document.getElementById('checkHTTP').addEventListener('click', async () => {
        const u = document.getElementById('httpInput').value.trim();
        const r = document.getElementById('resultHTTP');
        if (!u) { alert('Masukkan URL!'); return; }
        r.innerHTML = '<span class="warning">⏳ Cek status...</span>';
        let html = `<span class="label">🔄 HTTP: ${u}</span>\n──────────────────────\n\n`;
        try {
            const res = await fetch(u, { method:'HEAD' });
            html += `<span class="success">✅ Status   : ${res.status} ${res.statusText}</span>\n`;
            html += `<span class="success">🔗 URL Akhir: ${res.url}</span>\n`;
            html += `<span class="success">📦 Content-Type: ${res.headers.get('content-type')||'-'}</span>\n`;
        } catch(e) {
            html += `<span class="error">❌ Error: ${e.message}</span>\n\n`;
        }
        html += `\n<span class="info">🔗 Manual:</span>\n<a href="https://httpstatus.io/?url=${encodeURIComponent(u)}" target="_blank">🔗 HTTPStatus.io</a>\n<a href="https://www.redirect-checker.org" target="_blank">🔗 Redirect Checker</a>\n`;
        r.innerHTML = html;
    });

       // HASH GENERATOR
    document.getElementById('generateHash').addEventListener('click', async () => {
        const t = document.getElementById('hashInput').value;
        const r = document.getElementById('resultHash');
        if (!t) { alert('Masukkan teks!'); return; }
        const encoder = new TextEncoder();
        const data = encoder.encode(t);
        const buf2hex = b => [...new Uint8Array(b)].map(x => x.toString(16).padStart(2,'0')).join('');
        let html = `<span class="label">🔐 HASH: ${t}</span>\n──────────────────────\n\n`;
        try {
            const md5 = await crypto.subtle.digest('MD5', data).catch(()=>null);
            if (md5) html += `<span class="success">MD5    : ${buf2hex(md5)}</span>\n`;
        } catch(e) {}
        try {
            const sha1 = await crypto.subtle.digest('SHA-1', data);
            html += `<span class="success">SHA1   : ${buf2hex(sha1)}</span>\n`;
        } catch(e) {}
        try {
            const sha256 = await crypto.subtle.digest('SHA-256', data);
            html += `<span class="success">SHA256 : ${buf2hex(sha256)}</span>\n`;
        } catch(e) {}
        try {
            const sha512 = await crypto.subtle.digest('SHA-512', data);
            html += `<span class="success">SHA512 : ${buf2hex(sha512)}</span>\n`;
        } catch(e) {}
        r.innerHTML = html;
    });

    // JWT
    document.getElementById('decodeJWT').addEventListener('click', () => {
        const t = document.getElementById('jwtInput').value.trim();
        const r = document.getElementById('resultJWT');
        if (!t) { alert('Masukkan JWT!'); return; }
        const parts = t.split('.');
        if (parts.length !== 3) { r.innerHTML = '<span class="error">❌ JWT harus punya 3 bagian (header.payload.signature)</span>'; return; }
        let html = `<span class="label">🔓 JWT DECODED</span>\n──────────────────────\n\n`;
        try {
            const header = JSON.parse(atob(parts[0].replace(/-/g,'+').replace(/_/g,'/')));
            const payload = JSON.parse(atob(parts[1].replace(/-/g,'+').replace(/_/g,'/')));
            html += `<span class="info">📋 HEADER:</span>\n<span class="success">${JSON.stringify(header,null,2)}</span>\n\n`;
            html += `<span class="info">📦 PAYLOAD:</span>\n<span class="success">${JSON.stringify(payload,null,2)}</span>\n\n`;
        } catch(e) { html += `<span class="error">❌ ${e.message}</span>`; }
        r.innerHTML = html;
    });

    // PASSWORD STRENGTH
    document.getElementById('checkPassword').addEventListener('click', () => {
        const p = document.getElementById('passInput').value;
        const r = document.getElementById('resultPassword');
        if (!p) { alert('Masukkan password!'); return; }
        let score = 0;
        if (p.length >= 8) score++;
        if (p.length >= 12) score++;
        if (p.length >= 16) score++;
        if (/[a-z]/.test(p)) score++;
        if (/[A-Z]/.test(p)) score++;
        if (/[0-9]/.test(p)) score++;
        if (/[^a-zA-Z0-9]/.test(p)) score++;
        let strength = 'SANGAT LEMAH', color = 'error';
        if (score >= 7) { strength = 'SANGAT KUAT'; color = 'success'; }
        else if (score >= 5) { strength = 'KUAT'; color = 'success'; }
        else if (score >= 4) { strength = 'SEDANG'; color = 'warning'; }
        else if (score >= 2) { strength = 'LEMAH'; color = 'error'; }
        let html = `<span class="label">🔑 PASSWORD STRENGTH</span>\n──────────────────────\n\n`;
        html += `<span class="${color}">📊 Skor: ${score}/7</span>\n`;
        html += `<span class="${color}">💪 Kekuatan: ${strength}</span>\n`;
        html += `<span class="info">📏 Panjang: ${p.length}</span>\n`;
        html += `<span class="info">🔤 Lowercase: ${/[a-z]/.test(p)?'Ya':'Tidak'}</span>\n`;
        html += `<span class="info">🔠 Uppercase: ${/[A-Z]/.test(p)?'Ya':'Tidak'}</span>\n`;
        html += `<span class="info">🔢 Angka: ${/[0-9]/.test(p)?'Ya':'Tidak'}</span>\n`;
        html += `<span class="info">🔣 Simbol: ${/[^a-zA-Z0-9]/.test(p)?'Ya':'Tidak'}</span>\n`;
        r.innerHTML = html;
    });

    // IMAGE
    document.getElementById('checkImage').addEventListener('click', () => {
        const i = document.getElementById('imageInput').value.trim();
        const r = document.getElementById('resultImage');
        if (!i) { alert('Masukkan URL gambar!'); return; }
        let html = `<span class="label">🖼️ REVERSE: ${i}</span>\n──────────────────────\n\n`;
        html += `<a href="https://lens.google.com/uploadbyurl?url=${encodeURIComponent(i)}" target="_blank">🔗 Google Lens</a>\n`;
        html += `<a href="https://yandex.com/images/search?rpt=imageview&url=${encodeURIComponent(i)}" target="_blank">🔗 Yandex</a>\n`;
        html += `<a href="https://tineye.com/search?url=${encodeURIComponent(i)}" target="_blank">🔗 TinEye</a>\n`;
        html += `<a href="https://fotoforensics.com" target="_blank">🔗 FotoForensics</a>\n`;
        html += `<a href="https://exiftool.org" target="_blank">🔗 ExifTool</a>\n`;
        r.innerHTML = html;
    });

    // QR
    document.getElementById('generateQR').addEventListener('click', () => {
        const t = document.getElementById('qrInput').value.trim();
        const r = document.getElementById('resultQR');
        if (!t) { alert('Masukkan teks!'); return; }
        const qrUrl = `https://api.qrserver.com/v1/create-qr-code/?size=200x200&data=${encodeURIComponent(t)}`;
        r.innerHTML = `<span class="label">🎨 QR CODE</span>\n──────────────────────\n\n<img src="${qrUrl}" style="background:#fff;padding:10px;border-radius:10px;" />\n\n<a href="${qrUrl}" target="_blank" class="btn btn-sm">⬇️ Download QR</a>`;
    });

    // BASE64
    document.getElementById('encodeBase64').addEventListener('click', () => {
        const t = document.getElementById('base64Input').value;
        const r = document.getElementById('resultBase64');
        try { r.innerHTML = `<span class="label">🔒 BASE64 ENCODE</span>\n──────────────────────\n\n<span class="success">${btoa(t)}</span>`; } catch(e) { r.innerHTML = `<span class="error">❌ ${e.message}</span>`; }
    });
    document.getElementById('decodeBase64').addEventListener('click', () => {
        const t = document.getElementById('base64Input').value;
        const r = document.getElementById('resultBase64');
        try { r.innerHTML = `<span class="label">🔓 BASE64 DECODE</span>\n──────────────────────\n\n<span class="success">${atob(t)}</span>`; } catch(e) { r.innerHTML = `<span class="error">❌ ${e.message}</span>`; }
    });

    // URL
    document.getElementById('encodeURL').addEventListener('click', () => {
        const t = document.getElementById('urlInput2').value;
        const r = document.getElementById('resultURL');
        r.innerHTML = `<span class="label">🔒 URL ENCODE</span>\n──────────────────────\n\n<span class="success">${encodeURIComponent(t)}</span>`;
    });
    document.getElementById('decodeURL').addEventListener('click', () => {
        const t = document.getElementById('urlInput2').value;
        const r = document.getElementById('resultURL');
        try { r.innerHTML = `<span class="label">🔓 URL DECODE</span>\n──────────────────────\n\n<span class="success">${decodeURIComponent(t)}</span>`; } catch(e) { r.innerHTML = `<span class="error">❌ ${e.message}</span>`; }
    });

    // UUID
    document.getElementById('generateUUID').addEventListener('click', () => {
        const r = document.getElementById('resultUUID');
        const gen = () => 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, c => {
            const r = Math.random()*16|0;
            return (c === 'x' ? r : (r & 0x3 | 0x8)).toString(16);
        });
        let html = `<span class="label">🎲 UUID v4</span>\n──────────────────────\n\n`;
        for (let i=0; i<5; i++) html += `<span class="success">${gen()}</span>\n`;
        r.innerHTML = html;
    });

    // LOREM
    document.getElementById('generateLorem').addEventListener('click', () => {
        const c = parseInt(document.getElementById('loremCount').value) || 3;
        const r = document.getElementById('resultLorem');
        const words = 'lorem ipsum dolor sit amet consectetur adipiscing elit sed do eiusmod tempor incididunt ut labore et dolore magna aliqua'.split(' ');
        let html = `<span class="label">📝 LOREM IPSUM (${c} paragraf)</span>\n──────────────────────\n\n`;
        for (let i=0; i<c; i++) {
            let para = '';
            for (let j=0; j<50; j++) para += words[Math.floor(Math.random()*words.length)] + ' ';
            html += `<span class="success">${para.charAt(0).toUpperCase()+para.slice(1).trim()}.</span>\n\n`;
        }
        r.innerHTML = html;
    });

    // CASE
    document.getElementById('toUpper').addEventListener('click', () => {
        const t = document.getElementById('caseInput').value;
        document.getElementById('resultCase').innerHTML = `<span class="label">🔤 UPPERCASE</span>\n──────────────────────\n\n<span class="success">${t.toUpperCase()}</span>`;
    });
    document.getElementById('toLower').addEventListener('click', () => {
        const t = document.getElementById('caseInput').value;
        document.getElementById('resultCase').innerHTML = `<span class="label">🔤 LOWERCASE</span>\n──────────────────────\n\n<span class="success">${t.toLowerCase()}</span>`;
    });
    document.getElementById('toTitle').addEventListener('click', () => {
        const t = document.getElementById('caseInput').value;
        const r = t.toLowerCase().replace(/\b\w/g, c => c.toUpperCase());
        document.getElementById('resultCase').innerHTML = `<span class="label">🔤 TITLE CASE</span>\n──────────────────────\n\n<span class="success">${r}</span>`;
    });
    document.getElementById('toReverse').addEventListener('click', () => {
        const t = document.getElementById('caseInput').value;
        document.getElementById('resultCase').innerHTML = `<span class="label">🔤 REVERSE</span>\n──────────────────────\n\n<span class="success">${t.split('').reverse().join('')}</span>`;
    });

    // DUPLICATE
    document.getElementById('removeDup').addEventListener('click', () => {
        const t = document.getElementById('dupInput').value;
        const r = document.getElementById('resultDup');
        const lines = t.split('\n');
        const uniq = [...new Set(lines.map(l => l.trim()))].filter(Boolean);
        let html = `<span class="label">📋 HAPUS DUPLIKAT</span>\n──────────────────────\n\n`;
        html += `<span class="info">📊 Original: ${lines.length} baris</span>\n`;
        html += `<span class="success">✅ Setelah: ${uniq.length} baris</span>\n\n`;
        html += `<span class="success">${uniq.join('\n')}</span>`;
        r.innerHTML = html;
    });

    console.log('🔥 OSINT V3 READY');
</script>

</body>
</html>